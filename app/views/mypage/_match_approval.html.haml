.main_match_approval_container
  .match_approval_header
    %h2.title
      マッチング申請一覧
  %ul.match_approval_listsBox
    -# = @matches
    - if @matches == []
      .no_contents
        今はマッチング申請はありません
    - elsif @matches.present?
      - @matches.each do |match|
        = render partial: "mypage/li_render_list/user_line_list", locals: { match: match }
      
      

    -# = render "mypage/li_render_list/user_line_list"
    -# = render "mypage/li_render_list/user_line_list"
    -# = render "mypage/li_render_list/user_line_list"